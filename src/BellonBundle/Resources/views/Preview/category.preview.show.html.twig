{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field%}
    <div>
        {% if object | itm_ipw_exists('image') %}
            <img src="{{ object | itm_ipw_path('image') | imagine_filter('crop_preview') }}" />
        {% endif %}
    </div>
{% endblock %}